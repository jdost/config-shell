[[ $PATH != *$HOME/.local/bin* ]] && export PATH=~/.local/bin:$PATH
[[ ! -d $HOME/.local/bin ]] && mkdir -p $HOME/.local/bin

export AUR_HOME=$HOME/.local/aur
[[ ! -d "$AUR_HOME" ]] && mkdir -p $AUR_HOME
